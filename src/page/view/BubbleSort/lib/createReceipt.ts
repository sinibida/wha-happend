import { InitializeArgs, Receipt } from "../model/types";
import { stubReceipt } from "./_test";

export default function createReceipt(args: InitializeArgs): Receipt {
  // STUB
  console.log("TODO: createReceipt", args);
  return stubReceipt;
}
